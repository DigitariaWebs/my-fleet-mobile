EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
EXPO_PUBLIC_API_URL=http://localhost:4000
# Optional, defaults to ${EXPO_PUBLIC_API_URL}/auth
EXPO_PUBLIC_AUTH_BASE_URL=

# Stripe — publishable key only (pk_test_… / pk_live_…). Used to init
# <StripeProvider> before presenting the Payment Sheet for booking
# payments and deposit holds. Never put the secret or webhook secret here.
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# iOS only — required by Apple Pay if you enable it in the Payment Sheet.
EXPO_PUBLIC_STRIPE_MERCHANT_IDENTIFIER=
